/* global React, ReactDOM */
const Comp = () => (
  <div>Hello World</div>
);

/* for responsiveness reason */
const doms = document.getElementsByClassName('mount-point');
for (let i = 0; i < doms.length; i++) {
  ReactDOM.render(
    <Comp />,
    doms[i]
  );
}
