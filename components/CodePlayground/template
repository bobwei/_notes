/* scope with ${Object.keys(scope).join(', ')} */
const Comp = () => (
  <div>Hello World</div>
);

console.log('Hello World');

ReactDOM.render(
  <Comp />,
  document.getElementById(mountPointId)
);
